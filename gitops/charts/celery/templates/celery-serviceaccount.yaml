# Liée au rôle d'authentification kubernetes Vault "celery"
# (bound_service_account_names=celery-sa, bound_service_account_namespaces=celery
# — voir ansible/roles/vault/defaults/main.yml, vault_k8s_role_bindings.celery).
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Values.serviceAccountName }}
